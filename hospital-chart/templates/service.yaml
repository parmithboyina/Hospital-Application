apiVersion: v1
kind: Service
metadata:
  name: hospital-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: hospital-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
